class RobotAudio{constructor({onLevel=()=>{},onError=()=>{}}={}){this.onLevel=onLevel;this.onError=onError;this.stream=null;this.ctx=null;this.source=null;this.processor=null;this.zero=null;this.outCtx=null;this.outCursor=0;this.outSources=new Set()}async start(onChunk){if(this.stream)return;this.stream=await navigator.mediaDevices.getUserMedia({audio:{channelCount:1,echoCancellation:true,noiseSuppression:true,autoGainControl:true}});this.ctx=new(window.AudioContext||window.webkitAudioContext)();this.source=this.ctx.createMediaStreamSource(this.stream);this.processor=this.ctx.createScriptProcessor(4096,1,1);this.zero=this.ctx.createGain();this.zero.gain.value=0;this.processor.onaudioprocess=e=>{const input=e.inputBuffer.getChannelData(0);let sum=0;for(const v of input)sum+=v*v;this.onLevel(Math.sqrt(sum/input.length));onChunk(this.resample(input,this.ctx.sampleRate,16000))};this.source.connect(this.processor);this.processor.connect(this.zero);this.zero.connect(this.ctx.destination)}stop(){try{this.processor?.disconnect();this.source?.disconnect();this.zero?.disconnect();this.stream?.getTracks().forEach(t=>t.stop());this.ctx?.close()}catch(e){this.onError(e)}this.stream=null;this.ctx=null;this.source=null;this.processor=null;this.zero=null;this.onLevel(0)}resample(input,inRate,outRate){const ratio=inRate/outRate;const n=Math.max(1,Math.round(input.length/ratio));const out=new Int16Array(n);for(let i=0;i<n;i++){const start=Math.floor(i*ratio),end=Math.min(input.length,Math.floor((i+1)*ratio));let sum=0,c=0;for(let j=start;j<end;j++){sum+=input[j];c++}const s=Math.max(-1,Math.min(1,c?sum/c:input[Math.min(start,input.length-1)]));out[i]=s<0?s*0x8000:s*0x7fff}return out.buffer}toB64(buf){const b=new Uint8Array(buf);let s='';for(let i=0;i<b.length;i+=0x8000)s+=String.fromCharCode(...b.subarray(i,i+0x8000));return btoa(s)}async play(base64,mimeType='audio/pcm;rate=24000'){const m=/rate=(\d+)/.exec(mimeType||'');const rate=m?Number(m[1]):24000;if(!this.outCtx){this.outCtx=new(window.AudioContext||window.webkitAudioContext)();this.outCursor=this.outCtx.currentTime}if(this.outCtx.state==='suspended')await this.outCtx.resume();const raw=atob(base64),bytes=new Uint8Array(raw.length);for(let i=0;i<raw.length;i++)bytes[i]=raw.charCodeAt(i);const pcm=new Int16Array(bytes.buffer),floats=new Float32Array(pcm.length);for(let i=0;i<pcm.length;i++)floats[i]=pcm[i]/32768;const b=this.outCtx.createBuffer(1,floats.length,rate);b.copyToChannel(floats,0);const src=this.outCtx.createBufferSource();src.buffer=b;src.connect(this.outCtx.destination);const at=Math.max(this.outCtx.currentTime+.02,this.outCursor);src.start(at);this.outCursor=at+b.duration;this.outSources.add(src);src.onended=()=>this.outSources.delete(src)}stopPlayback(){for(const s of this.outSources){try{s.stop()}catch(_){}}this.outSources.clear();if(this.outCtx)this.outCursor=this.outCtx.currentTime}}window.RobotAudio=RobotAudio;
